/// Adds two to the number given.
/// # Examples
///
/// ```
/// let arg = 5;
/// let answer = add_two::add_two(arg);
/// assert_eq!(7, answer);
/// ```
pub fn add_two(a: i32) -> i32 {
    a + 2
}
