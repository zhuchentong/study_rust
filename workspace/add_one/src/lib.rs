/// Adds one to the number given.
/// # Examples
/// ```
/// let arg = 5;
/// let answer = add_one::add_one(arg);
/// assert_eq!(6, answer);
/// ```
pub fn add_one(a: i32) -> i32 {
    a + 1
}
