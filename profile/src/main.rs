use profile::add;
use profile::multiply;
fn main() {
    let result = add(1, 2);
    println!("Add Result: {}", result);

    let result = multiply(2, 3);
    println!("Multiply Result: {}", result);
}
